This script was written in Python, using the Pyserial library.
This library let us configurate the serial port that we are going to open and the parameters we already used in the stm32 c code.
It is very simple, only for testing purposes
